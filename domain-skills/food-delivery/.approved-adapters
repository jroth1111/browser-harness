# food-delivery stealth_session wraps browser_harness.stealth_helpers which
# is the approved package-level import. The calling scripts import from this
# local wrapper. The token "stealth_session" matches the module filename and
# function name but the actual implementation delegates to browser_harness.
#
# discover_apis.py uses urllib.request in try_http_replay() for replaying
# discovered API endpoints with harvested cookies - an adapter pattern.
scripts/lib/stealth_session.py
scripts/discover_apis.py
scripts/enumerate_restaurants.py
scripts/extract_menus.py
